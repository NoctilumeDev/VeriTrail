# VeriTrail 0.12.2 Release Notes

> 状态：`RELEASE CANDIDATE / PENDING PUBLIC READBACK`
>
> 当前公开稳定 Core：`0.12.1`
>
> 候选坐标：`0.12.2`

VeriTrail 0.12.2 是 Core 0.12.1 之后的有界维护候选。它只修复一个公开组合路径：`veritrail demo`
在同级 staging 中创建 Catalog、再将整个 demo 原子发布到最终目录后，Catalog 仍必须从出生时就绑定
最终 Artifact root，使公开 wheel 生成的 demo 能直接交给官方 `catalog-serve`。

本候选不重开 M0–M14，不改变 Catalog Schema、`catalog_id`、Run identity、Verdict、公共
`build_catalog()` 语义或 Workbench，也不移动或改写 `v0.12.0`、`v0.12.1`。验证器仍然 fail-closed；
完整搬移已经发布的 demo 后，`catalog-serve` 仍必须精确拒绝 `ARTIFACT_ROOT_MISMATCH`。

## 根因与修复边界

0.12.1 的 demo producer 在 staging 中扫描 Artifact root 并写入位置摘要，然后才原子改名。Bundle、
Verdict、Catalog 内容与 `catalog_id` 没有漂移，过期的是 producer 冻结的物理位置绑定。0.12.2 让
Catalog 模块内部的受限入口验证 `stage/artifacts -> FINAL/artifacts` 同父目录映射，并在扫描 staging
内容时只计算一次最终 Artifact root 摘要，同值写入 SQLite 与 Manifest。

受限入口拒绝不同父目录、错误相对布局、已有最终目标、symlink／reparse staging 或 Artifact root，
以及 Windows 会改写的尾点／尾空格名称。未预期内部异常以稳定 `DEMO_INTERNAL_ERROR` 非零退出，
不输出 traceback 或原始本机路径。精确授权范围和既有边界见
[Core demo Catalog 最终位置绑定维护合同](74-core-demo-catalog-binding-maintenance-contract.md)。

## 已取得的实现证据

最小 producer 修复在实现提交 `bf964be6daef874ebd828ae26c9a21449097e246` 上经
[PR #13](https://github.com/NoctilumeDev/VeriTrail/pull/13) 的七项 required checks 全部通过，合并为
`ff3cb2756ebc03859062706de14f05a9cbfe7881`。该精确合并提交随后通过：

- [Public CI 33112063798](https://github.com/NoctilumeDev/VeriTrail/actions/runs/33112063798)；
- [Browser Smoke 33112063789](https://github.com/NoctilumeDev/VeriTrail/actions/runs/33112063789)：五项真实浏览器检查完成，
  HTTP error 为零，端口释放。

这些事实证明实现已进入公开 `main` 并通过现有门禁；它们不证明下面尚未创建的 0.12.2 标签、资产
或公开下载副本已经成立。发布候选自身还必须经过新的受保护 PR 与精确 post-merge 读回。

## 发布停止线

候选必须继续完成：

1. 新的受保护 PR required checks 与精确 post-merge Public CI／Browser Smoke；
2. 从最终发布提交重新构建 wheel、sdist、Workbench ZIP 和验证摘要，不复用旧 staging 资产；
3. 创建受保护的注释标签 `v0.12.2`，并读回 tag object 与解引用提交；
4. 从 GitHub Release 公开下载全部资产，逐项核对大小、GitHub digest 与 SHA-256；
5. 在仓库外 clean Python 3.10／3.13 环境只安装公开 wheel，并真实运行
   `demo -> catalog-serve -> health/Catalog/Bundle`；
6. 从公开 sdist clean install 后重复适用链；完整移动 demo 的负对照仍必须得到
   `ARTIFACT_ROOT_MISMATCH`；
7. 结束后确认服务进程、端口、SQLite sidecar、staging 与临时验证目录按所有权合同闭合。

本轮不运行 Codex Security 深度扫描、攻击路径验证或极端攻击工作流；这些工作流仍按既定决定封存，
不得由普通 CI 绿灯暗示为已经完成。

## 计划发布资产

```text
veritrail-0.12.2-py3-none-any.whl
veritrail-0.12.2.tar.gz
veritrail-workbench-0.12.2.zip
core-v0.12.2-validation-summary.json
SHA256SUMS.txt
```

`SHA256SUMS.txt` 继续采用非自指约定，只覆盖其余四个 payload；它自身必须由上传副本与公开下载副本
逐字节比较。上述资产当前只是冻结计划，不是已发布事实。完成公开读回以前，README、START_HERE
和安装命令继续把 `0.12.1` 作为公开稳定入口。
