# Post-M8 收束路线 Plan v1

> 状态：`FROZEN`（规划基线；实时执行进度见第 0 节）
> 影响层级：`L3_SYSTEM`（仅规划，不修改代码、Schema、裁决或 M0–M8 冻结标签）
> Plan v1 原始前置基线：M0–M8 均为 `FROZEN`，当时最新标签为 `m8-v0.9.0`
> 当前实施基线：M9 已以 `m9-v0.10.0` @ `3181d69` 冻结；M10 当前补丁基线为
> `m10-v0.11.1` @ `f4efdd2`，历史 `m10-v0.11.0` @ `0084443` 保持不动
> 冻结引用：`post-m8-plan-v1`
> 当前实施门禁：M10、M11 全部门禁与远端读回完成；M12–M14 仍为 `PLANNED`

## 0. 当前执行进度（冻结后注记）

本节只同步 Plan v1 的执行进度，不改写 `post-m8-plan-v1` 在冻结时的目标、顺序、边界或证据要求。

| Milestone | Capability | Current status | Current ref |
| --- | --- | --- | --- |
| M9 | 受控项目命令执行 | `FROZEN` | `m9-v0.10.0` @ `3181d69` |
| M10 | 有界完整项目自举 | `FROZEN` | `m10-v0.11.1` @ `f4efdd2` |
| M11 | 真实项目功能全链路 | `FROZEN` | `m11-v0.12.0` @ `b13e2fb`；本文第 7 节与文档 23、25、26、27 |
| M12 | 故宫主题前端终稿 | `PLANNED` | 本文第 8 节 |
| M13 | 系统思维与分层代码质量终审 | `PLANNED` | 本文第 9 节 |
| M14 | 整改后终局复验与发布收束 | `PLANNED` | 本文第 10 节 |

M9 已完成代码、自动化、真实命令、浏览器、物理键盘、资源、安全、清理和 GitHub/tag 读回。
M10 已完成合同与设计验证，并实现 Profile/Plan/Preview、listener、逐节点长运行 Job、owned HTTP
readiness、双节点串行启动和 best-effort 逆序清理组件，以及严格 `runtime.bootstrap` 四附件、同时
封存 Plan/Profile 的 Bundle、Catalog/Comparison 身份复核和 Pairing/Batch 显式拒绝；Run-owned
work/staging、pre-teardown 脱敏封存、subject 指纹与资源分账也已有 observed-run。真实 Browser 已接入
公共 Plan 0.6 `run` 的批准且预检 `PROCEED` 路径，正向与业务失败负向均生成可被 Catalog 验真的
不可变 Bundle，并完成逆序清理。`STOP_ESCALATION/ABORT` 已在零被测进程下生成仅含 preflight 的
`ABORTED/PENDING` Bundle；其余公共退出、失败封存、Catalog 隔离和 Workbench 通用账册读回也已完成。
M10 地基审查已发现并修复报告重推导、readiness 所有权和只读文件稳定读取三个接缝，严格串行轮也
已完成。随后 16 GB 有界压力轮从 Wave A 完整重跑，独立度 1/2/3、取消交错、1000 总请求、
Bundle/Catalog 与零残留门禁全部成立。发布安全整改又封闭 11 条原攻击路径和资源硬限制，最终候选
完成严格串行 13/13、双 Python 228/228、Workbench 58/58、最终压力与 Codex 内置浏览器复验；
`0084443` 与 `m10-v0.11.0^{}` 已从 GitHub 读回一致，状态为 `FROZEN`。
冻结后地基纠偏又完成双 Python 262/262、Workbench 59/59、公共出口、16 GB 有界压力、生产浏览器、
Windows 原子发布和清理复验；首次补丁读回时 `main` 与 `m10-v0.11.1^{}` 均指向 `f4efdd2`。M11
候选适配性盘点因此必须引用新补丁基线，不能继续继承旧标签的已知缺口。

M11 的冻结后候选盘点与探索事实见文档 22；用户确认后的单节点能力与真实项目双门 Contract 0.3
及 Gate B Plan v2 纠偏 Contract 0.4 见文档 23，Gate A、v1 失败与 v2 验证事实见文档 25–27。Gate B
已严格串行形成四 Run、Comparison、Catalog/Workbench、内置浏览器与双 Python 证据；最终发布门禁
与远端标签读回已关闭，M11 以 `m11-v0.12.0` 冻结。M12 尚未进入实现。未来 M13 独立合同起草时，
审查发现固定分为 `BLOCKER / MUST_FIX / DEFERRED`，禁止为了追求零 warning 或零 smell 进行无关
全仓重写；该规则同样只作为后继合同门禁，不倒填为 Plan v1 冻结时已经存在的条款。

## 1. 产品判断

VeriTrail 是本地优先、资源感知、证据优先的**验证脚手架**，也是一个**认知放大器**。
它把原本隐含在操作者脑中的变量、控制组、不变量、停止线、失败事实和结论边界，转换为可重复、
可审计、可证伪的实验过程。

它不会自动产生正确认知。错误的变量选择、遗漏的不变量或过宽的结论，同样会被稳定地放大。
因此 Post-M8 的目标不只是“能运行更多命令”，而是让真实项目执行仍然服从 M0–M8 已证明的
方法论和安全边界。

必须持续成立的认知护栏：

- 严格区分 `OBSERVED`、`INFERRED` 与 `NOT_PROVEN`，不得把调度计划写成运行事实；
- 计划、断言、停止线和裁决规则必须先于观察冻结；观察后修改必须产生新版本；
- 失败、反例、中止、污染和未解释异常必须保留，不能只归档成功路径；
- AI 可以解释、聚类或建议下一步，但不能决定确定性 `PASS / FAIL`；
- 结论必须携带代码、环境、资源、适用范围和未知项，不能脱离证据包单独传播；
- 工具自身的资源和采集扰动必须与被测对象分开记录。

## 2. Plan v1 冻结时的事实与剩余缺口

M0–M8 已证明从 sealed Plan 到全因子批次派生分析的本地纵向链路，但还没有证明 VeriTrail
能够安全接管一个普通项目从冷状态到验收结束的完整生命周期。

Post-M8 的主线缺口是：

1. **受控项目命令执行**：当前只有内置 `STATIC_HTTP`，没有 npm、Maven、Python、Docker
   等项目命令的结构化执行能力；
2. **有界完整项目自举**：当前轻量自举只覆盖 VeriTrail 自身目录，不覆盖项目发现、依赖准备、
   服务启动、就绪、恢复和逆序清理；
3. **真实项目全链路**：尚未用一个不同于测试夹具的项目贯通预检、执行、浏览器、多实例、
   裁决、归档和清理；
4. **前端终稿**：当前主题已形成方向，但还没有完成全局色彩、留白、状态语义、响应式和
   无障碍收束；
5. **系统与代码终审**：尚未在完整能力形成后，从所有者、消费者、状态机、数据流、失败边界
   和爆炸半径俯瞰整个系统；
6. **终局复验**：尚未在整改后的候选版本上完成冷启动、代表性真实链路和无残留收束。

计划编辑器、在线写、远程执行、生产集群编排、统计显著性和 AI 裁决不是本路线的默认目标。
如需纳入，必须单独上浮范围并重新评审，不能借前端收尾或命令执行顺手加入。

## 3. 总体顺序

```text
P0 规划与系统设计门
  -> M9 受控项目命令执行
  -> M10 有界完整项目自举
  -> M11 真实项目功能全链路
  -> M12 故宫主题前端终稿
  -> M13 系统思维与分层代码质量终审
  -> M14 整改后终局复验与发布收束
```

在 Plan v1 冻结时，M9–M14 均登记为 `PLANNED`。P0 冻结后仍需逐个建立并冻结独立合同；每一
里程碑必须完成代码事实、自动化、真实运行、适用的浏览器/多实例、安全与清理证据，并冻结为
可寻址基线，下一里程碑才能进入实现。

前端视觉规范可以提前规划，但 M12 的实现必须等待 M11 功能事实稳定。这样可以先固定视觉语法，
又不会为了装修界面而反复改动尚未收束的生命周期和数据语义。

各阶段的能力所有权必须互斥：

| 阶段 | 只负责 | 明确不负责 |
| --- | --- | --- |
| M9 | 结构化一次性命令 | 长运行服务、依赖图、自举和 UI 写操作 |
| M10 | Windows 11/C1 的本地进程生命周期 | C2/C3、跨平台、Docker 和真实项目终局结论 |
| M11 | 用已有能力验证不同类型真实项目 | 为目标项目临时发明通用适配器或改写旧裁决 |
| M12 | `L0_PRESENTATION` 视觉与交互可达性 | 计划编辑、在线写、执行控制和 Schema 变化 |
| M13 | 系统/代码审查与有界整改门禁 | 借审查加入新功能或无关全仓重写 |
| M14 | 在已证明支持矩阵内复验和发布收束 | 新平台、新冷状态、新适配器或新产品能力 |

## 4. P0：规划与系统设计门

P0 只产生合同和验收矩阵，不产生运行代码。进入 M9 前必须冻结：

- Post-M8 的目标、非目标和术语；
- 受控命令的信任边界、威胁模型与允许列表语义；
- 项目自举的冷状态分级、生命周期状态机、所有权和清理责任；
- 首个已证明平台、平台适配边界，以及 `SUPPORTED / NOT_PROVEN` 口径；
- 项目 Profile、环境发现、版本核验和依赖准备之间的权威关系；
- 首个真实目标项目和第二种不同类型项目的选择标准；
- 16 GB Windows 主机的资源预算、串行默认和微并行上限；
- M9–M14 各自的正向、反向、故障恢复和退出矩阵；
- 故宫主题的 CSS 设计合同与无障碍底线；
- 系统终审、代码质量审查和最终发布的停止条件。

P0 冻结前允许修改本文；冻结后若改变命令信任模型、自举状态机或最终结论语义，必须创建
Plan v2，不能静默改写 Plan v1。

## 5. M9：受控项目命令执行

### 5.1 目标

让不同项目能够声明并运行自己的构建、静态检查、测试和一次性任务，同时保持结构化、可预览、
可中止、可脱敏、可追溯和可清理。

“任意项目命令”表示**项目可以登记不同工具和参数**，不表示 VeriTrail 接受无约束 Shell
字符串。

### 5.2 最小合同

一个命令声明至少需要包含：

- 稳定命令 ID、用途和所属项目 Profile；
- 可执行文件引用与参数数组，禁止用字符串拼接替代参数边界；
- 受项目根目录约束的工作目录；
- 允许读取的环境变量名称；秘密值只透传，不记录、不回显；
- 超时、预期退出码、最大输出和资源停止线；
- 固定 `execution_kind=ONESHOT`；长运行服务必须拒绝并留给 M10；
- 中止、进程树回收、证据保存和清理策略；
- 执行前结构化预览及 sealed Plan 绑定。

### 5.3 安全不变量

- 默认拒绝 Shell 字符串、命令替换、未解析变量和根目录外工作目录；
- 默认拒绝通过 `cmd /c`、PowerShell `-Command`、`bash -c`、`python -c`、`node -e` 等解释器
  入口绕过结构化参数与允许列表；确有必要时必须另立显式适配器合同；
- 默认拒绝未登记可执行文件及未经预览的参数变化；
- 不持久化 `.env` 值、令牌、Cookie、认证头、私钥或个人绝对路径；
- 中止必须终止本次拥有的完整进程树，不能误杀无关进程；
- 清理只能作用于本次 Run 明确拥有并已核验的资源；
- stdout、stderr、退出码、时序、资源与脱敏动作都进入 Manifest；
- 退出码 `0` 只是观察事实，不能自动提升为整个 Run 的 `PASS`；最终裁决仍由 sealed 断言负责；
- 资源保护触发时 ExecutionStatus 为 `ABORTED`；没有独立硬失败证据时 Verdict 保持 `PENDING`，
  两个维度不能压扁或伪造成 `PASS / FAIL`。

### 5.4 冻结出口

- 至少两种不同命令家族分别串行完成真实正向运行，不能用同一批并行结果代替独立证据；
- 覆盖超时、非零退出、输出超限、秘密回显拦截、工作目录越界和用户中止；
- 每条异常路径均形成证据并确认进程、端口和临时资源清理；
- 旧 M0–M8 Bundle 与 CLI 兼容回归通过；
- 不在 M9 顺手加入服务依赖图、Docker 编排或在线命令编辑器。

## 6. M10：有界完整项目自举

### 6.1 目标

在 M9 受控执行之上，把项目从**已声明且有边界的冷状态**推进到可验收状态，并在成功、失败或
中止后恢复到可解释的清洁状态。

这里的“完整”只表示声明范围内的生命周期闭环完整，不表示 VeriTrail 要成为操作系统安装器、
通用版本管理器、包管理器或生产集群编排器。

候选编排阶段如下；这些是 M10 内部阶段，不新增或替代公共 ExecutionStatus：

```text
DISCOVERED
  -> PREFLIGHTED
  -> PREPARED
  -> DEPENDENCIES_READY
  -> SERVICES_READY
  -> EXERCISED
  -> EVIDENCE_FINALIZED
  -> TEARDOWN_COMPLETE
```

任一阶段都可以进入 `ABORTING -> EVIDENCE_FINALIZED -> TEARDOWN_COMPLETE`。失败不能跳过证据
封存，证据封存也不能阻止尽力清理。

### 6.2 冷状态分级

| 级别 | 起始事实 | Plan v1 定位 |
| --- | --- | --- |
| `C0 RUNNING` | 外部依赖或项目服务已经运行 | 可以兼容，但不作为完整自举证明 |
| `C1 PROCESS_COLD` | 合格运行时与项目依赖已存在，没有项目服务运行 | M10 首个冻结目标 |
| `C2 WORKSPACE_COLD` | 合格运行时已存在，项目依赖尚未按锁文件恢复 | C1 冻结后另立合同 |
| `C3 HOST_COLD` | 所需语言运行时、系统工具或中间件尚未安装 | Plan v1 明确不支持 |

M10 不自动安装系统级 Python、Node、Java、Maven、Docker 或中间件，不擅自改变 PATH、代理、
注册表、系统服务或全局包。发现 C3 缺口时停止进入启动阶段：ExecutionStatus 为 `ABORTED`，
没有独立硬失败证据时 Verdict 为 `PENDING`，并形成结构化证据和人工处置建议；不能为了继续
运行而扩大权限。

### 6.3 权威关系、平台与适配器

- 项目 Profile 是工具、版本、端口、服务、就绪条件、依赖顺序和回收顺序的权威声明；
- 自动发现只能提出候选，预检负责核验声明，不能自动挑选“看起来可用”的版本；
- 版本不兼容时拒绝进入启动阶段；不得静默降级、自动安装或改写 Profile；
- 执行前必须把发现事实、声明差异和最终确定性计划共同展示并封存；
- 首个可冻结平台是当前真实运行的 Windows 11；Linux 与 macOS 在各自真实运行前保持
  `NOT_PROVEN`，不得用“代码看起来可移植”冒充支持；
- 平台差异通过明确的进程、路径、端口和信号适配层隔离；新增平台是独立实验变量，不能和新
  项目类型、新依赖类型同时引入；
- M10 不要求为了“纯标准库”重复实现成熟系统能力；新增第三方依赖必须有必要性、安全、维护、
  许可证和资源审查，并进入锁文件与证据；
- 首个纵向切片使用宿主机本地进程适配器。Docker 不是作弊，也不是默认答案；只能在本地进程
  基线冻结后作为独立 Profile/适配器验证，不能同时改变平台和项目类型；
- 本地进程、Docker 或中间件必须分别使用明确适配器，不能全部退化成 Shell；
- 只停止当前 Run 创建或明确接管的资源，不接管来源不明的既有服务；
- Docker 适配器不得全局清理镜像、卷、网络或其他项目容器；
- 就绪必须使用有界健康检查，不能把“进程存在”当成“服务可用”；
- 逆序回收后检查进程、端口、容器、临时目录和连接残留。

### 6.4 资源策略

- 每次运行重新采样内存、磁盘、端口和相关运行时，不能照搬机器档案快照；
- 默认串行；微并行必须声明 wave、总预算、互斥资源和停止条件；
- M8 的 wave 只是调度语义，不能作为 M10 真实重叠运行已经成立的证据；
- 超过软阈值停止升压，超过硬阈值立即保存现场并中止；
- VeriTrail 自身资源与被测项目资源分别记账。

### 6.5 首个纵向切片与冻结出口

- Windows 11、`C1 PROCESS_COLD`、一个 sealed 项目 Profile；
- 一个由 Run 拥有的本地依赖进程、一个应用进程和一种有界就绪探针；
- 串行完成一次成功自举、一次中途失败恢复和一次用户中止恢复；
- 证明就绪检查、逆序清理、端口竞争和已有外部进程保护；
- 第二次运行不受第一次残留污染；
- C2/C3、Linux、macOS、Docker 和真实重叠运行继续明确为 `NOT_PROVEN`；不为了扩大标签而在
  同一里程碑同时引入多个变量。

## 7. M11：真实项目功能全链路

### 7.1 目标

用真实项目证明 VeriTrail 不只会验证自己的夹具。目标项目必须在实施前按类型、资源、可重复性、
浏览器链路和失败注入能力登记；不能因为某个项目更容易通过而在观察结果后更换。

至少需要两层目标：

- 自身最小目标：用于快速证明执行器和自举基本闭环；
- 不同类型的真实目标：用于排除“VeriTrail 只适配 VeriTrail”的自证循环。

只读盘点完成后，M11 Contract 0.3 已固定先交付目标无关的 Windows/C1 单 application Gate A，再以
固定实现验证 `InkNarratives @ b443a1c967bbc4c50f1bec7ece62abc4c4196fdb` 的 Gate B。Gate B v1 已
保留首个真实失败，Contract 0.4 只把目标交互计划升为 v2；v2 随后按原顺序完成四个真实 Run，
两次正向 Comparison 为 `MATCH`，且没有为目标修改 Core、裁决或固定 Subject ref。

### 7.2 代表性完整链路

```text
实时环境与资源预检
  -> sealed Plan 与命令预览
  -> 有界完整项目自举
  -> 构建 / 检查 / 测试 / 服务生命周期
  -> 桌面与移动浏览器真实操作
  -> Console / Network / 关键业务事实
  -> 适用的多实例、重试、故障与恢复
  -> 确定性 Verdict
  -> 适用的 Comparison / Paired / Batch 派生分析
  -> 证据归档与哈希
  -> 逆序清理与残留确认
```

### 7.3 冻结出口

- 成功链、业务失败链、基础设施失败链和恢复链全部真实运行；
- 浏览器检查关键交互、失败重试、桌面/移动、Console 与 Network；
- 涉及多实例或一致性时核对权威事实、重复副作用和恢复结果；
- 失败事实与成功终验共同保留；
- 复跑能够解释稳定项、差异项和仍未证明项；
- Comparison、PairedAnalysis 和 BatchAnalysis 只在真实问题具有对应的复跑、反事实或组合语义时
  预注册使用；不得为了“能力全亮”制造无意义处理组或伪因果结论；
- 如果目标项目需要尚未冻结的通用执行器、平台或生命周期适配器，M11 必须停止并回到独立合同，
  不能把目标特例硬编码进 Core；
- 16 GB 资源不足可以形成 `PENDING`，不能降低一致性或安全标准。

## 8. M12：故宫主题前端终稿

### 8.1 实现原则

- 使用 HTML/CSS 和现有组件体系构造，不贴背景图、不用位图纹理模拟建筑；
- 中国式留白依靠结构、比例和虚实关系，不用整体降对比、灰雾化或柔焦制造“东方感”；
- 色彩允许内部张力，避免把全部明度、饱和度和色温压成单一的日韩式现代极简模板；
- 视觉修改不得重新解释 Verdict、Schema、状态机或证据所有权。
- M12 保持 Workbench 的既有数据所有权，不加入计划编辑、在线写、命令启动/停止或报告发布；
  若未来需要控制台能力，必须作为新的系统级里程碑评审。

### 8.2 状态色语义

- 宫墙朱红：仅用于失败、异常、故障断点和危险操作；
- 琉璃鎏金：用于运行中、加载中和正在校验；
- 玉白：主背景、正文与只读证据区域，保持明确白度而不是雾灰；
- 黛青：导航、边界、分区轴线与次要控件；
- 成功、待定、中止和不可归因必须同时有文字、图形或纹理差异，不能只靠颜色。

### 8.3 布局与验收

- 总览、链路详情、原始证据/归档形成层层递进但不过度拟物的院落式层级；
- 留白服务于信息分组和阅读节奏，不能以减少必要事实为代价；
- 键盘顺序、焦点环、语义标签、减少动画和屏幕阅读保持有效；
- 复合页签等标准控件通过明确的视觉成组、选中态与焦点层级降低按键模式切换的认知成本，同时保留
  `Tab` 进入控件组、方向键在组内移动的标准键盘语义；
- 对比度至少满足 WCAG AA 的适用要求；
- 真实检查桌面与移动视口、缩放、长文本、空状态、错误状态和加载状态；
- Codex 内置浏览器完成完整用户链，DevTools Console/Network 无未解释异常。

## 9. M13：系统思维与分层代码质量终审

系统思维审查设置两道门：P0 在实施前检查方向和边界，M13 在能力形成后检查真实系统。前者避免
走错路，后者防止每个模块局部通过却没有形成可靠整体。

### 9.1 系统审查维度

- 产品目标、非目标和真实用户链是否仍一致；
- Plan、Run、Evidence、Verdict、Catalog 与三类派生分析的所有权和数据流；
- 命令、自举、浏览器、资源与清理状态机是否存在绕过路径；
- 失败、重试、中止、崩溃恢复和部分成功是否都有唯一且可解释的语义；
- 安全边界、秘密流向、工作目录、进程树、端口和 Docker 所有权；
- 单机资源预算是否贯穿计划、运行、分析和 UI；
- 冻结合同、兼容策略和迁移路径是否仍能被旧消费者使用；
- 是否出现为了“通用”而复制 CI、容器平台或生产编排器的问题。

### 9.2 分层代码质量审查

- `L0 表现层`：语义、可访问性、渲染性能和前端状态边界；
- `L1 组件内部`：职责、错误处理、测试质量、资源释放和可维护性；
- `L2 公共契约`：Schema、序列化、兼容性、消费者和迁移；
- `L3 系统级`：状态机、跨组件不变量、信任边界、爆炸半径和恢复能力。

审查发现先按严重性、所有者和影响面归档，再决定最小整改范围。不得为了统一风格进行无关的
全仓重写；跨层整改必须列出全部消费者并复跑对应矩阵。

M13 不是隐藏的功能开发里程碑。若发现需要改变冻结 Schema、命令信任模型、自举状态机或裁决
语义的 L2/L3 问题，必须标记受影响基线失效并回到独立合同；不能在“代码质量修复”名义下静默
重构后直接进入 M14。

## 10. M14：整改后终局复验与发布收束

M14 不增加功能，只验证整改后的候选版本并形成最终结论。

必须覆盖：

- Windows 11、`C1 PROCESS_COLD` 支持矩阵内的冷启动和有界完整自举；
- 一个自身目标和一个不同类型真实目标的代表性完整链路；
- 浏览器桌面/移动、键盘、Console/Network 和失败恢复；
- 适用的受控微并行、多实例、重复、超时、中止和恢复；
- Bundle 重建、哈希和 Catalog 必须复验；Comparison、PairedAnalysis 与 BatchAnalysis 使用既有
  兼容回归夹具全部复验，真实目标只运行预注册且语义适用的派生分析；
- 全程资源曲线、软/硬停止线和最终无残留；
- 安装、使用、安全、限制、故障排查和里程碑历史文档；
- 代码、标签、发布产物和远端仓库的一致性回读。

最终报告只能在证据范围内声明完成。任何必需链路因资源、权限或外部状态无法运行时，项目状态
保持 `PENDING`，并明确列出已完成实现、局部证据与待执行终验。

## 11. Plan v1 冻结条件

以下冻结条件已经在 2026-08-11 逐条完成规划一致性核对：

- 用户确认 Post-M8 的顺序、范围和非目标；
- M9–M14 的能力所有权没有交叉实施或以终验名义扩张范围；
- M9 结构化命令合同没有 Shell 后门或含糊的资源所有权；
- M10 明确 C0–C3 冷状态、Windows-first 证据口径，并保证每个失败出口都能进入证据封存和清理；
- M11 真实目标选择规则不会形成自证循环；
- M12 视觉合同明确区分中国式结构留白与整体雾化，并锁定状态色语义；
- M13 审查层级与整改回归责任明确；
- M14 终局标准没有用局部测试代替用户可见完整链路；
- 全部阶段均遵守 16 GB 主机的串行或受限微并行边界。

冻结 Plan v1 当时只允许 M9 进入独立合同起草，不授权实现；M9 合同必须先冻结。当前执行事实
以后续注记和各独立里程碑合同为准，不能反向改写 Plan v1 当时尚未实现或验收的历史事实。
